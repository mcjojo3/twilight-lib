architectury {
    minecraft = rootProject.property('1_20_1_minecraft_version')
}

subprojects {
    apply plugin: 'dev.architectury.loom'

    loom {
        silentMojangMappingsLicense()
    }

    archivesBaseName = "twilight_lib-1.20.1-${project.name}"

    dependencies {
        minecraft "com.mojang:minecraft:${rootProject.property('1_20_1_minecraft_version')}"
        mappings loom.officialMojangMappings()
    }

    tasks.withType(JavaCompile) {
        options.release = 17
    }
}
