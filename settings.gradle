// DEPRECATED: This root settings file is no longer used.
// Each module (forge-1.20.1, neoforge-1.21.1) is now a standalone Gradle project
// with its own settings.gradle file.
//
// See README-STRUCTURE.md for the new mono-repo structure.

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.minecraftforge.net' }
        maven { url = 'https://maven.neoforged.net/releases' }
        maven { url = 'https://repo.spongepowered.org/repository/maven-public/' }
        mavenCentral()
    }
}

rootProject.name = 'twilight-lib'

// Modules are no longer included here - they are independent projects
// include 'forge-1.20.1'
// include 'neoforge-1.21.1'
